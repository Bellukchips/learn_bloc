name: firebase_todos_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  firebase_core: ^1.6.0
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.0
  todo_repository:
    path: repository/todo_repository
  user_repository:
    path: repository/user_repository

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  bloc: ^7.2.0
  flutter_bloc: ^7.3.0
flutter:
  uses-material-design: true
